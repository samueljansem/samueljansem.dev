---
export type Props = {
  date: string;
};

const { date } = Astro.props;
---

<div class="relative pl-10">
  <div class="absolute left-1.5 top-1.5 w-3 h-3 rounded-full -zag-bg zag-transition"></div>
  <p class="zag-muted text-sm font-medium mb-2">{date}</p>
  <slot />
</div>
